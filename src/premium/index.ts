// Compatibility shim for older imports.
// Public core builds must not depend on ignored /src/pro implementation files.
export { createNoopPremiumProviderRegistry as createDefaultPremiumProviderRegistry } from "./providers/defaultPremiumProviderRegistry.js";
